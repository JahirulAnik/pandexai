---
name: Bug report
about: A command produced wrong output, crashed, or gave a confusing error
labels: bug
---

**Command you ran**
e.g. `/pandex clean sales.csv` or `python scripts/gather.py a.csv b.csv`

**What happened**
Paste the JSON output or error message.

**What you expected**

**Sample data**
A minimal CSV/XLSX/JSON that reproduces it (strip anything sensitive). Even 3-5 rows helps a lot.

**Environment**
- OS:
- Python version (`.pandex/venv/bin/python --version`):
- pandas version (`.pandex/venv/bin/python -c "import pandas;print(pandas.__version__)"`):
- pandexai version (`npx pandex --version` or `package.json`):
